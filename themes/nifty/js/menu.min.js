var listItems = $("#mainnav-menu li");
listItems.each(function(idx, li) {
    var item = $(li);
    if ($('a', this).attr('href') === window.location.href) {
        item.addClass("active-link");
    }
});